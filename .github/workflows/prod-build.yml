name: Prod deploy (manual / on release tag)

on:
  push:
    tags: [ "v*" ]
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  build:
    name: dbt build --target prod
    runs-on: ubuntu-latest
    env:
      SNOWFLAKE_ACCOUNT:      ${{ secrets.SNOWFLAKE_ACCOUNT }}
      DBT_USER:               ${{ secrets.DBT_USER }}
      PRIVATE_KEY:            ${{ secrets.PRIVATE_KEY }}
      PRIVATE_KEY_PASSPHRASE: ${{ secrets.PRIVATE_KEY_PASSPHRASE }}
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "pyproject.toml"
      - run: uv python install 3.13
      - run: uv sync
      - name: Install dbt profile
        run: |
          mkdir -p ~/.dbt
          cp airbnb/_profiles/profiles.yml ~/.dbt/profiles.yml
      - run: cd airbnb && uv run dbt deps
      - run: cd airbnb && uv run dbt build --target prod
      - name: Upload prod target as artifact
        uses: actions/upload-artifact@v4
        with:
          name: prod-target
          path: airbnb/target/
          retention-days: 90
