selectors:
  - name: dim_except_listings_w_hosts
    description: Selects all dim models, excluding dim_except_listings_w_host
    definition:
      union:
        - method: path
          value: models/dim
        - exclude:
          - method: fqn
            value: dim_listings_w_host 